{
  "name": "@maiertech/prettier-config",
  "version": "0.2.1",
  "author": "Thilo Maier <git@maier.asia>",
  "description": "Sharable config for Prettier.",
  "repository": "github:maiertech/prettier-config",
  "license": "MIT",
  "keywords": [
    "prettier"
  ],
  "main": "dist/index.js",
  "module": "dist/index.modern.js",
  "engines": {
    "node": ">=10.20.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "prepare": "microbundle --format modern,cjs",
    "watch": "microbundle watch --format modern,cjs --compress false",
    "version": "changeset version && npm run format",
    "release": "changeset publish",
    "lint": "npm run lint:eslint && npm run lint:prettier",
    "lint:eslint": "eslint . --ignore-path .gitignore",
    "lint:prettier": "npm run prettier -- --check",
    "prettier": "prettier . --ignore-path .gitignore",
    "format": "npm run lint:eslint && npm run:prettier",
    "format:eslint": "npm run lint:eslint -- --fix",
    "format:prettier": "npm run prettier -- --write",
    "purge": "rm -rf ./node_modules",
    "upgrade-interactive": "npx npm-check -u"
  },
  "peerDependencies": {
    "prettier": "^2.0.5"
  },
  "devDependencies": {
    "@changesets/cli": "^2.12.0",
    "@maiertech/eslint-config": "^0.16.0",
    "eslint": "^7.17.0",
    "husky": "^4.3.7",
    "lint-staged": "^10.5.3",
    "microbundle": "^0.12.4",
    "micromatch": "^4.0.2",
    "npm-check": "^5.9.2",
    "prettier": "^2.2.1"
  }
}
